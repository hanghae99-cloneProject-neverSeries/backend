const express = require("express");
const router = express.Router();
const ctrlIndex = require("../controller/ctrl.index");

// router.post("/posts", ctrlIndex.getProcess.getPost); //게시글 조회

module.exports = router;
